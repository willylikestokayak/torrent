
<nav>
<div class="nav-wrapper">
  <a href="#" class="brand-logo header">Torrent</a>
  <ul id="nav-mobile" class="right hide-on-med-and-down">
    <li><%= link_to 'Torrent', controller: 'articles' %></li>
    <li><%= link_to 'Write New', new_article_path %></li>
    <li><%= link_to 'LogOut', logout_path %></li>
  </ul>
</div>
</nav>

<p class='description'>The journey to becoming a full stack  </p>

<h1>Articles </h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th colspan="3"></th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= link_to 'Read Full Article', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Delete', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>